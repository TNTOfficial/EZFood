export interface MainSlider {
  id: string;
  title: string;
  description?: string;
  linkName?: string;
  link?: string;
  imageUrl: string;
  status: boolean;
  createdAt: string;
  updatedAt?: string;
}
